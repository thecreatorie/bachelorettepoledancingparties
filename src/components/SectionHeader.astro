---
export interface Props {
  eyebrow?: string;
  title: string;
  description?: string;
  align?: 'left' | 'center';
  number?: string;
}
const { eyebrow, title, description, align = 'left', number } = Astro.props;
---

<header class={`section-header section-header--${align}`}>
  {number && <span class="ghost-num" aria-hidden="true">{number}</span>}
  {eyebrow && <p class="eyebrow">{eyebrow}</p>}
  <h2>{title}</h2>
  {description && <p class="description">{description}</p>}
</header>

<style>
  .section-header {
    position: relative;
    margin-block: var(--space-section) var(--space-8);
    max-width: 60ch;
  }
  .section-header--center {
    text-align: center;
    margin-inline: auto;
  }
  .ghost-num {
    position: absolute;
    top: -0.62em;
    left: -0.04em;
    z-index: -1;
    font-family: var(--font-display);
    font-weight: 200;
    font-size: clamp(5rem, 4rem + 8vw, 10rem);
    line-height: 1;
    letter-spacing: -0.02em;
    color: color-mix(in oklab, var(--color-secondary) 8%, transparent);
    pointer-events: none;
    user-select: none;
  }
  .section-header--center .ghost-num {
    left: 50%;
    transform: translateX(-50%);
  }
  /* Never let the numeral sit behind words on small screens. */
  @media (max-width: 767px) {
    .ghost-num { display: none; }
  }
  .eyebrow {
    margin: 0 0 var(--space-3) 0;
    font-size: 0.75rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.2em;
    color: var(--color-secondary);
  }
  h2 {
    margin: 0;
  }
  .description {
    margin-top: var(--space-3);
    color: var(--color-text-muted);
    font-size: var(--fs-lg);
  }
</style>
